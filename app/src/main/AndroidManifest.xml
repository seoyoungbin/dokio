<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dokio.tacademy.pedokio">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission-sdk-23 android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/doicon"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/doicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name="com.dokio.tacademy.pedokio.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.dokio.tacademy.pedokio.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.dokio.tacademy.pedokio.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name="com.dokio.tacademy.pedokio.SearchViewActivity" />
        <activity android:name="com.dokio.tacademy.pedokio.SearchListActivity" />
        <activity android:name="com.dokio.tacademy.pedokio.DetailActivity" />
        <activity android:name="com.dokio.tacademy.pedokio.LoginActivity" />
        <activity android:name="com.dokio.tacademy.pedokio.FilterActivity" />
        <activity android:name="com.dokio.tacademy.pedokio.SignUpActivity" />
        <activity android:name="com.dokio.tacademy.pedokio.PetUploadActivity" />
        <activity android:name="com.dokio.tacademy.pedokio.PetUploadDetailActivity" />
        <activity android:name="com.dokio.tacademy.pedokio.PetListActivity" />
        <activity android:name="com.dokio.tacademy.pedokio.DiaryActivity"></activity>
    </application>

</manifest>